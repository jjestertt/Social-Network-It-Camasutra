import React from 'react';
import style from './Settings.module.css';

const Settings = () => {
    return(
        <div className="settings">
            <h2 className={style.title}>This is settings</h2>
        </div>
    )
}
export default Settings;